# Plan: Remove EventDays widgets, ensure Event edit has per-event stats

## Current State

- **`EventStatsWidget`** (`Events/Widgets/EventStatsWidget.php`) — already wired to `EditEvent` page as a header widget. Shows 5 per-event stats: Дни, События, Спикеры, Гости, Прогресс. This is exactly what the user wants.
- **`ScheduleProgressWidget`** (`EventDays/Widgets/ScheduleProgressWidget.php`) — exists but is **NOT wired** to any page (unused dead code). Shows global aggregate stats.
- **`ListEventDays`** — has no `getHeaderWidgets()`, so no widgets displayed.
- **`EditEventDay`** — bare-bones, no widgets.

## What to Do

### 1. Delete `ScheduleProgressWidget`
- **File**: `/Users/pavelklimov/Herd/landing/app/Filament/Resources/EventDays/Widgets/ScheduleProgressWidget.php`
- This widget is unused (not wired to any page) and shows global stats, not per-event stats. Remove it.

### 2. No changes needed to `EditEvent` / `EventStatsWidget`
- `EventStatsWidget` already shows exactly the 5 stats the user requested (speakers, guests, days, events, progress) scoped to the current event.
- It's already wired as a header widget on `EditEvent.php`.

## Files Modified
- Delete: `app/Filament/Resources/EventDays/Widgets/ScheduleProgressWidget.php`

## Files NOT Modified (already correct)
- `app/Filament/Resources/Events/Widgets/EventStatsWidget.php` — already has per-event stats
- `app/Filament/Resources/Events/Pages/EditEvent.php` — already wires the widget

## Verification
- Visit `/admin/event-days` — should show list with no header widgets
- Visit `/admin/events/{id}/edit` — should show header stats widget with 5 cards (Дни, События, Спикеры, Гости, Прогресс) scoped to that event
- Run `php artisan route:list` to ensure no broken references
