<?php

return [

    /*
    |--------------------------------------------------------------------------
    | OnlyOffice Document Server
    |--------------------------------------------------------------------------
    |
    | URL публичного Document Server (тот, что доступен браузеру администратора,
    | например https://docs.example.ru). Внутри контейнера может отличаться.
    |
    */

    'url' => env('ONLYOFFICE_URL', 'http://localhost:8081'),

    /*
    | Общий секрет JWT между Document Server и нашим приложением.
    | Должен совпадать со значением JWT_SECRET в конфигурации OnlyOffice.
    | Если пусто — подпись отключена (callback будет отклоняться).
    */
    'secret' => env('ONLYOFFICE_JWT_SECRET', ''),

    /*
    | Время жизни токенов (секунды).
    */
    'token_ttl' => (int) env('ONLYOFFICE_JWT_TTL', 3600),

    /*
    | Базовый URL совеого приложения (для document.url и callbackUrl).
    | Берётся из APP_URL, но может быть переопределён (например, сменить
    | http на https при нулле реверс-прокси).
    */
    'app_url' => rtrim(env('ONLYOFFICE_APP_URL', env('APP_URL', 'http://localhost')), '/'),

    /*
    | Высота окна редактора в пикселях.
    | По умолчанию — высота страницы A4 при 96 dpi (297 мм ≈ 1123 px).
    | Реальная высота считается в JS: page + chrome, но не больше доступного
    | места в окне браузера (авто-подгонка под экран).
    */
    'editor_height' => (int) env('ONLYOFFICE_EDITOR_HEIGHT', 1123),

    /*
    | Приблизительная высота внутренней панели OnlyOffice (тулбар + вкладки),
    | которую надо добавить к странице, чтобы лист был виден целиком.
    */
    'editor_chrome' => (int) env('ONLYOFFICE_EDITOR_CHROME', 140),

    /*
    | Высота шапки админки + отступы страницы Filament, которые вычитаются
    | из высоты окна браузера при авто-подгонке.
    */
    'editor_header_offset' => (int) env('ONLYOFFICE_EDITOR_HEADER_OFFSET', 260),

    /*
    | Ширина окна редактора в пикселях.
    | По умолчанию — ширина страницы A4 при 96 dpi (210 мм ≈ 794 px).
    | Окно центрируется по странице.
    */
    'editor_width' => (int) env('ONLYOFFICE_EDITOR_WIDTH', 794),
];